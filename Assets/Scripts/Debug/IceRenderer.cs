using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class IceRenderer : MonoBehaviour
{
    // Start is called before the first frame update
    //Camera cam;
    //public RenderTexture target;
    //void Start()
    //{
    //    cam = gameObject.GetComponent<Camera>();
    //    cam.targetTexture = target;
    //    gameObject.transform.parent = SceneView.GetAllSceneCameras()[0].transform;
    //    gameObject.transform.localPosition = Vector3.zero;
    //    gameObject.transform.localRotation = Quaternion.identity;
    //    gameObject.transform.localScale = Vector3.one;
    //
    //    cam.fieldOfView = Camera.main.fieldOfView;
    //    cam.nearClipPlane = Camera.main.nearClipPlane;
    //    cam.farClipPlane = Camera.main.farClipPlane;
    //
    //    int ccm = cam.cullingMask;
    //    int newmask = ~(1 << 3);
    //    cam.cullingMask = ccm & newmask;
    //}
    //
    //// Update is called once per frame
    //void Update()
    //{
    //    
    //}
}
